package com.qa.encapsulation;
/*
 * we have variables and methods in a class
 * when we encapsulate variables within the method is called encapsulation
 * we have encapsulate our variable in company.java(we have public +private variable and on top of that we have created wrapper i.e getter and setter). 
 * can we encapsulate our methods? yes
 * when we encapsulate data members(data members can be a variable or a method) with a capsule is called encapsulation.
 * Private variable+private method encapsulated with a public layer(method).User can access public layer and indirectly use private variable and method. 
 */
public class EncapsulationConcept {

}
